services:
  - type: web
    name: rag-api
    runtime: python
    buildCommand: pip install uv && uv sync
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: SUPABASE_DB_URL
        sync: false
